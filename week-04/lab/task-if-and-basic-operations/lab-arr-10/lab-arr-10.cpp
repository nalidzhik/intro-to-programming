// TODO later
#include <iostream>
#include <vector>
#include <iomanip>
#include<algorithm>

using namespace std;

void getPrintMatrix(int, int);

int main()
{
	int n;
	cin >> n;
	int m;
	cin >> m;

	getPrintMatrix(n, m);
    return 0;
}

void getPrintMatrix(int n, int m)
{
	vector<int> numbers;
	vector<int> rows;
}